---
phase: 12-database-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/AUDIT_DATABASE.md
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04

must_haves:
  truths:
    - "A developer can look up any model and see every field with its type, nullability, default, and business purpose"
    - "A developer can trace any relationship -- from Model A to Model B -- and know the FK field, cardinality, and cascade behavior"
    - "A developer can determine which fields store USD, which store ARS, and how ExchangeRate connects to transactions"
    - "A developer can answer 'what gets logged in AuditLog and when' from the document alone"
    - "A developer can find every enum value and understand its domain meaning"
    - "A developer can answer 'what happens when I delete a Development?' (cascade effects) from this document"
  artifacts:
    - path: "docs/AUDIT_DATABASE.md"
      provides: "Complete database architecture audit document"
      contains: "# Auditoria de Base de Datos"
  key_links:
    - from: "docs/AUDIT_DATABASE.md"
      to: "prisma/schema.prisma"
      via: "Every model, field, relation, index, and enum in schema.prisma is documented"
      pattern: "model User|model Sale|model CashMovement|model AuditLog"
    - from: "docs/AUDIT_DATABASE.md"
      to: "src/lib/exchange-rate.ts"
      via: "Currency section explains how exchange rates are fetched and used"
      pattern: "ExchangeRate|USD|ARS|cotizacion"
    - from: "docs/AUDIT_DATABASE.md"
      to: "src/server/actions/audit-log.actions.ts"
      via: "Audit trail section documents what triggers audit entries"
      pattern: "AuditLog|createAuditLog|audit"
---

<objective>
Produce `docs/AUDIT_DATABASE.md` -- a comprehensive database architecture document analyzing the full Prisma schema: every model field-by-field, all relationships with FK/cascade details, indexes, constraints, dual USD/ARS currency modeling, enum reference, and audit trail implementation.

Purpose: Enable any developer to fully understand the data model without reading schema.prisma -- every table, every field, every relationship, every business rule encoded in the database layer.

Output: One file `docs/AUDIT_DATABASE.md`, written entirely in Spanish.
</objective>

<execution_context>
@D:/Proyectos/sistemaInmobiliaria/.claude/get-shit-done/workflows/execute-plan.md
@D:/Proyectos/sistemaInmobiliaria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-database-architecture/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AUDIT_DATABASE.md -- Full schema analysis with models, relationships, currency, and audit trail (DB-01, DB-02, DB-03, DB-04)</name>
  <files>docs/AUDIT_DATABASE.md</files>
  <read_first>
    prisma/schema.prisma
    src/server/models/user.model.ts
    src/server/models/development.model.ts
    src/server/models/lot.model.ts
    src/server/models/sale.model.ts
    src/server/models/installment.model.ts
    src/server/models/extra-charge.model.ts
    src/server/models/cash-movement.model.ts
    src/server/models/cash-balance.model.ts
    src/server/models/exchange-rate.model.ts
    src/server/models/payment-receipt.model.ts
    src/server/models/person.model.ts
    src/server/models/signing.model.ts
    src/server/models/notification.model.ts
    src/server/models/message.model.ts
    src/server/models/audit-log.model.ts
    src/server/models/tag.model.ts
    src/server/models/role-permission.model.ts
    src/server/models/system-config.model.ts
    src/server/models/cron.model.ts
    src/lib/exchange-rate.ts
    src/lib/installment-recalculator.ts
    src/lib/audit.ts
    src/server/actions/audit-log.actions.ts
    src/server/actions/exchange-rate.actions.ts
  </read_first>
  <action>
Read ALL files listed in read_first. The Prisma schema (720 lines, 20 models) is the primary input. Model files show how Prisma queries are structured (includes, where, orderBy patterns). Currency and audit files show runtime behavior.

Create `docs/AUDIT_DATABASE.md` with the following structure. The ENTIRE document must be written in Spanish. Use full file paths on first mention, short names after.

**Document structure (exact sections, in this order):**

## 1. Encabezado y Metadatos
- Title: `# Auditoria de Base de Datos -- Sistema Inmobiliaria`
- Date, version, purpose statement
- Reference to primary source: `prisma/schema.prisma`

## 2. Resumen del Schema
- Total models, total enums, total indexes/constraints
- Database: PostgreSQL
- ORM: Prisma with `@db.Decimal(x,y)` for financial fields
- Brief overview of domain groupings (Auth, Desarrollo, Ventas, Pagos/Caja, Firmas, Comunicaciones, Sistema)

## 3. Referencia de Enums (consolidated)
Document EVERY enum in the Prisma schema. For each enum:
- Enum name
- All values with business meaning (e.g., `VENDIDO` -- "Lote con venta activa asociada")
- Which model fields use this enum

Enums to document (all from schema.prisma): Role, DevelopmentType, LotStatus, PersonType, SaleStatus, PaymentMethod, CashMovementType, ExtraChargeStatus, SigningStatus, NotificationType, plus any others found.

## 4. Modelos por Dominio (DB-01 -- field-by-field)
Group models by domain area. For EACH model, write a subsection with:

### a) Proposito del modelo
One paragraph explaining the business purpose.

### b) Tabla de campos
Table format with columns: Campo | Tipo | Nullable | Default | Descripcion
Include EVERY field. For relation fields (e.g., `development Development @relation(...)`) document the FK field separately from the relation.

### c) Relaciones
List every relationship with: direction (this model -> other or other -> this), cardinality (1:1, 1:N, N:M), FK field name, and onDelete/onUpdate cascade behavior.

### d) Indexes y constraints
List all `@@index`, `@@unique`, `@@map` annotations. Explain business reason for each index.

### e) Reglas de negocio en el schema
Any default values, constraints, or patterns that encode business rules (e.g., `status LotStatus @default(DISPONIBLE)` means lots start as available).

**Domain groupings (document models in this order):**

**Auth y Usuarios:**
1. User -- system users with RBAC roles
2. RolePermission -- DB-backed permission overrides

**Desarrollos:**
3. Development -- real estate developments
4. Lot -- lots within developments
5. Tag -- tagging system
6. LotTag -- many-to-many join table (if it exists as explicit model)

**Ventas:**
7. Person -- clients and suppliers
8. Seller -- sales agents (if exists as model; check schema)
9. Sale -- sales with lifecycle states
10. Installment -- scheduled payment installments
11. ExtraCharge -- extraordinary charges/refuerzos

**Pagos y Caja:**
12. CashMovement -- unified cash movements (14+ types)
13. CashBalance -- running balance tracking
14. PaymentReceipt -- auto-generated payment receipts
15. ExchangeRate -- daily USD/ARS exchange rates

**Firmas:**
16. SigningSlot -- signing appointment scheduling

**Comunicaciones:**
17. Message -- internal messages
18. MessageRecipient -- message delivery tracking
19. Notification -- system notifications/alerts

**Sistema:**
20. AuditLog -- operation audit trail
21. SystemConfig -- system configuration key-value store

NOTE: The schema has ~20 models. Document ALL of them, even if it means 21+ subsections. Do not skip any model found in schema.prisma.

## 5. Mapa Global de Relaciones (DB-02)
Global summary table with columns: Modelo A | Relacion | Modelo B | Campo FK | onDelete | onUpdate
Include EVERY relationship from the entire schema. This should be a single comprehensive table a developer can scan to understand the full relationship graph.

## 6. Arquitectura de Moneda Dual USD/ARS (DB-03)
Dedicated section explaining dual-currency modeling:

### a) Campos que almacenan USD
List every field across all models that stores USD amounts (e.g., Sale.totalPrice, Installment.amount, etc.). Include the model name, field name, Prisma type, and decimal precision.

### b) Campos que almacenan ARS
List every field that stores ARS amounts (e.g., CashMovement amounts in pesos).

### c) Modelo ExchangeRate
How the exchange rate is stored (date, rate, source). How it connects to payment transactions.

### d) Flujo de conversion
How a USD-denominated installment gets paid in ARS: the exchange rate lookup, multiplication, and which fields store which part. Reference `src/lib/exchange-rate.ts` for the fetching logic.

### e) Precision decimal
Document every `@db.Decimal(X,Y)` annotation in the schema. Explain why specific precision matters for financial operations.

## 7. Implementacion del Audit Trail (DB-04)
Dedicated section about AuditLog:

### a) Estructura del modelo AuditLog
Field-by-field (already covered in section 4, but here provide additional context about the JSON fields).

### b) Que se audita
What entities/operations trigger audit log entries. Reference `src/server/actions/audit-log.actions.ts` and `src/lib/audit.ts` to document which server actions call the audit logger.

### c) Datos capturados
What data is stored in the audit entry: user who performed action, entity type, entity ID, action type, before/after state (if applicable), timestamp.

### d) Consulta y retencion
How audit logs are queried (from audit-log.model.ts), any pagination/filtering patterns, retention strategy (if any).

## 8. Observaciones y Patrones
- Common patterns observed across models (e.g., all models have createdAt/updatedAt, soft delete patterns if any, UUID vs autoincrement)
- Potential concerns or notable design choices discovered during analysis
- Cross-reference to docs/AUDIT_CONCEPT.md for business context

**Writing guidelines:**
- Use layered depth: overview paragraph first, then detail tables
- Include actual Prisma field definitions where they clarify meaning
- Cross-reference between sections (e.g., "Ver seccion 6.1 para detalle de campos USD")
- No diagrams -- use prose + tables
- Be exhaustive: every field, every relation, every index. This is an AUDIT, not a summary.
  </action>
  <verify>
    <automated>bash -c 'FILE="D:/Proyectos/sistemaInmobiliaria/docs/AUDIT_DATABASE.md"; test -f "$FILE" && echo "FILE_EXISTS" && wc -l "$FILE" | awk "{if (\$1 > 500) print \"LENGTH_OK:\"\$1; else print \"LENGTH_SHORT:\"\$1}" && echo "=== Model Coverage ===" && grep -c "^### .*model\|^### .*Model\|^### [0-9]" "$FILE" && echo "=== Enum Coverage ===" && grep -ci "enum\|Role\|LotStatus\|SaleStatus\|CashMovementType" "$FILE" && echo "=== Currency Section ===" && grep -c "USD\|ARS\|Decimal\|ExchangeRate" "$FILE" && echo "=== Audit Section ===" && grep -c "AuditLog\|audit\|createAuditLog" "$FILE"'</automated>
  </verify>
  <acceptance_criteria>
    - File docs/AUDIT_DATABASE.md exists
    - Document is at least 600 lines long (exhaustive field-by-field coverage of 20 models)
    - Contains section header "# Auditoria de Base de Datos"
    - Contains "Referencia de Enums" section listing ALL enums from schema.prisma
    - Contains a subsection for EVERY model in schema.prisma (all 20+)
    - Each model subsection has: purpose paragraph, field table, relationships list, indexes
    - Contains "Mapa Global de Relaciones" section with comprehensive FK table
    - Contains "Arquitectura de Moneda Dual" section with USD fields list, ARS fields list, conversion flow
    - Contains "Implementacion del Audit Trail" section with what-triggers-audit detail
    - Contains "Observaciones y Patrones" section
    - All field tables use: Campo | Tipo | Nullable | Default | Descripcion format
    - Written entirely in Spanish
    - References actual file paths (prisma/schema.prisma, src/server/models/*, src/lib/*)
    - No TODO/TBD/placeholder text
  </acceptance_criteria>
  <done>
    docs/AUDIT_DATABASE.md exists with 8 major sections: schema overview, enum reference, all models grouped by domain with field tables, global relationship map, dual-currency architecture, audit trail implementation, and observations. Every model, every field, every relationship, every index documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate document completeness against schema and fix gaps</name>
  <files>docs/AUDIT_DATABASE.md</files>
  <read_first>
    docs/AUDIT_DATABASE.md
    prisma/schema.prisma
  </read_first>
  <action>
Read the generated AUDIT_DATABASE.md and validate completeness against the actual Prisma schema. Fix any gaps found.

**Checklist 1 -- Model coverage:**
Parse prisma/schema.prisma and extract every `model X {` declaration. Verify EVERY model has a dedicated subsection in the document. If any model is missing, add it with full field table.

**Checklist 2 -- Field coverage per model:**
For the 5 most complex models (Sale, CashMovement, Installment, Lot, Person), cross-check that EVERY field from the Prisma model appears in the document's field table. If any field is missing, add it.

**Checklist 3 -- Enum coverage:**
Extract every `enum X {` from schema.prisma. Verify each enum is documented in the "Referencia de Enums" section with ALL its values. If any missing, add them.

**Checklist 4 -- Relationship completeness:**
Verify the "Mapa Global de Relaciones" table includes ALL relationships (check @relation annotations in schema.prisma). Every `@relation` should have a row in the global table. Count relations in schema vs rows in table.

**Checklist 5 -- Currency fields:**
Verify every `@db.Decimal` field in schema.prisma is listed in the currency section (section 6). Cross-reference to confirm USD vs ARS classification is correct.

**Checklist 6 -- Index coverage:**
Verify every `@@index` and `@@unique` annotation from schema.prisma is documented in the appropriate model subsection.

**Checklist 7 -- No placeholders:**
Search for TODO, TBD, PENDING, [placeholder], "..." in the document. Remove any found and replace with actual content.

Fix any gaps found. Do NOT rewrite sections that are already correct -- only add missing items.
  </action>
  <verify>
    <automated>bash -c 'FILE="D:/Proyectos/sistemaInmobiliaria/docs/AUDIT_DATABASE.md"; SCHEMA="D:/Proyectos/sistemaInmobiliaria/prisma/schema.prisma"; echo "=== Doc Lines ==="; wc -l "$FILE"; echo "=== Models in Schema ==="; grep -c "^model " "$SCHEMA"; echo "=== Models in Doc (subsections) ==="; grep -c "^### [0-9]" "$FILE"; echo "=== Enums in Schema ==="; grep -c "^enum " "$SCHEMA"; echo "=== Field Tables ==="; grep -c "Campo.*Tipo.*Nullable\|Campo.*Tipo.*Default" "$FILE"; echo "=== Relation Map Rows ==="; grep -c "onDelete\|CASCADE\|SetNull\|Restrict" "$FILE"; echo "=== Decimal Fields ==="; grep -c "Decimal" "$FILE"; echo "=== No Placeholders ==="; grep -ci "TODO\|TBD\|\[pending\]\|\[placeholder\]" "$FILE" || echo "0"'</automated>
  </verify>
  <acceptance_criteria>
    - Number of model subsections >= number of `model X` declarations in schema.prisma
    - All fields of Sale, CashMovement, Installment, Lot, Person models present in their field tables
    - All enums from schema.prisma documented with all their values
    - Global relationship table row count matches @relation count in schema
    - All @db.Decimal fields listed in currency section
    - All @@index and @@unique annotations documented
    - Zero occurrences of TODO/TBD/PENDING/placeholder
    - Document line count >= 600 lines
  </acceptance_criteria>
  <done>
    docs/AUDIT_DATABASE.md is validated as complete against the actual Prisma schema. Every model, every field, every enum, every relationship, every index is accounted for. No gaps or placeholders remain.
  </done>
</task>

</tasks>

<verification>
Final phase verification checks:
1. `docs/AUDIT_DATABASE.md` exists and is at least 600 lines
2. Document contains all 8 major sections (schema overview, enums, models by domain, relationships, currency, audit trail, observations)
3. Every model from prisma/schema.prisma has a dedicated subsection with field table
4. Every enum is documented with all values
5. Global relationship map covers all FKs with cascade behaviors
6. Currency section enumerates every USD and ARS field with decimal precision
7. Audit trail section documents triggers, captured data, and query patterns
8. Written entirely in Spanish
9. No TODO/TBD/placeholder text remaining
</verification>

<success_criteria>
A developer who has never seen this codebase can read docs/AUDIT_DATABASE.md and:
1. Look up any model and see every field with type, nullability, default, and business meaning
2. Trace any relationship between models including FK field, cardinality, and cascade behavior
3. Answer "which fields store USD vs ARS?" definitively from the currency section
4. Answer "what gets audited and what data is captured?" from the audit trail section
5. Understand every enum value's business meaning
6. Answer "what happens when I delete entity X?" from cascade documentation
</success_criteria>

<output>
After completion, create `.planning/phases/12-database-architecture/12-01-SUMMARY.md`
</output>
