---
phase: 02-financial-logic-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/fixtures/installment-fixtures.ts
  - src/__tests__/unit/lib/installment-generator.test.ts
autonomous: true
requirements: [FIN-01, FIN-02, FIN-03, FIN-04, FIN-09]

must_haves:
  truths:
    - "generateInstallments clamps collectionDay 31 to last day of short months (Feb 28/29, Apr 30, Jun 30, Sep 30, Nov 30)"
    - "generateInstallments handles year rollover when installments cross Dec-Jan boundary"
    - "generateInstallments uses firstInstallmentAmount for installment 1 when provided"
    - "generateInstallments returns empty array for totalInstallments=0 (contado sale)"
    - "All monetary assertions use expectMoney, never raw toBe"
  artifacts:
    - path: "src/__tests__/fixtures/installment-fixtures.ts"
      provides: "Named test scenarios shared across all financial test files"
      exports: ["STANDARD_60_CUOTAS", "VARIABLE_FIRST_INSTALLMENT", "DAY_31_CLAMPING", "LEAP_YEAR_FEB", "NON_LEAP_YEAR_FEB", "YEAR_ROLLOVER", "CONTADO_SALE", "ARS_CURRENCY", "toGeneratorParams"]
    - path: "src/__tests__/unit/lib/installment-generator.test.ts"
      provides: "Unit tests for generateInstallments covering FIN-01 through FIN-04"
      min_lines: 80
  key_links:
    - from: "src/__tests__/unit/lib/installment-generator.test.ts"
      to: "src/lib/installment-generator.ts"
      via: "import { generateInstallments }"
      pattern: "import.*generateInstallments.*from.*installment-generator"
    - from: "src/__tests__/unit/lib/installment-generator.test.ts"
      to: "src/__tests__/fixtures/installment-fixtures.ts"
      via: "import named scenarios"
      pattern: "import.*from.*fixtures/installment-fixtures"
    - from: "src/__tests__/unit/lib/installment-generator.test.ts"
      to: "src/__tests__/helpers/money.ts"
      via: "import { expectMoney }"
      pattern: "import.*expectMoney.*from.*helpers/money"
---

<objective>
Create shared test fixtures and write unit tests for `generateInstallments()` covering date edge cases (FIN-01, FIN-02), variable first installment (FIN-03), zero installments (FIN-04), and financial precision (FIN-09).

Purpose: These are the highest-risk pure functions in the financial domain. The generator directly produces installment records stored in the database -- a bug here corrupts payment schedules for every sale.

Output: Shared fixtures file reusable by all Phase 2 test files + comprehensive generator test suite.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-financial-logic-tests/02-CONTEXT.md
@.planning/phases/02-financial-logic-tests/02-RESEARCH.md
@.planning/phases/01-testing-infrastructure/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/installment-generator.ts:
```typescript
interface GenerateInstallmentsParams {
  saleId: string;
  totalInstallments: number;
  regularInstallmentAmount: number;
  firstInstallmentAmount?: number;
  firstInstallmentMonth: string; // "YYYY-MM"
  collectionDay: number;
  currency: Currency;
}

interface InstallmentData {
  saleId: string;
  installmentNumber: number;
  amount: number;
  currency: Currency;
  dueDate: Date;
  monthLabel: string;
}

export function generateInstallments(params: GenerateInstallmentsParams): InstallmentData[];
```

From src/lib/sale-helpers.ts:
```typescript
export const MONTH_NAMES = ["ENERO", "FEBRERO", ..., "DICIEMBRE"] as const;
```

From src/__tests__/helpers/money.ts:
```typescript
export function expectMoney(received: number, expected: number): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared fixtures file with named scenarios</name>
  <files>src/__tests__/fixtures/installment-fixtures.ts</files>
  <action>
Create the shared fixtures file at `src/__tests__/fixtures/installment-fixtures.ts` with named test scenarios mapped to requirements. Follow the exact pattern from RESEARCH.md Code Examples section.

Required exports:
1. `SharedInstallmentParams` interface -- base params shared by generator and preview
2. `toGeneratorParams(shared, saleId?, currency?)` -- extends shared params with saleId + currency for generator calls
3. Named scenarios (all using realistic business amounts per user decision):
   - `STANDARD_60_CUOTAS` -- 60 installments, $250 each, collectionDay 10, starting 2025-01 (base scenario)
   - `VARIABLE_FIRST_INSTALLMENT` -- 12 installments, regular $250, first $500, collectionDay 15, starting 2025-03 (FIN-03)
   - `DAY_31_CLAMPING` -- 12 installments, $200 each, collectionDay 31, starting 2025-01 (FIN-01)
   - `LEAP_YEAR_FEB` -- 3 installments, $300 each, collectionDay 31, starting 2024-01 (FIN-01 leap)
   - `NON_LEAP_YEAR_FEB` -- 3 installments, $300 each, collectionDay 31, starting 2025-01 (FIN-01 non-leap)
   - `YEAR_ROLLOVER` -- 6 installments, $500 each, collectionDay 15, starting 2025-10 (FIN-02)
   - `CONTADO_SALE` -- 0 installments, amount 0, collectionDay 10, starting 2025-01 (FIN-04)
   - `ARS_CURRENCY` -- 6 installments, $50,000 ARS each, collectionDay 10, starting 2025-01 (currency passthrough)

Import the `Currency` type from `@/generated/prisma/client/client` for the `toGeneratorParams` function.

Pin dates: 2024 for leap year tests, 2025 for non-leap year tests (per user decision).
  </action>
  <verify>npx vitest run --reporter=verbose 2>&1 | tail -5 (existing 9 tests still pass, no import errors from new file)</verify>
  <done>Fixtures file exists with all 8 named scenarios exported, toGeneratorParams helper works, no type errors</done>
</task>

<task type="auto">
  <name>Task 2: Write generateInstallments test suite (FIN-01, FIN-02, FIN-03, FIN-04)</name>
  <files>src/__tests__/unit/lib/installment-generator.test.ts</files>
  <action>
Create the test file at `src/__tests__/unit/lib/installment-generator.test.ts`. Import `generateInstallments` from `@/lib/installment-generator`, named fixtures from `@/__tests__/fixtures/installment-fixtures`, and `expectMoney` from `@/__tests__/helpers/money`.

Structure with describe blocks per requirement:

**FIN-01: collectionDay 31 clamping in short months**
- Test with `DAY_31_CLAMPING` fixture: verify Feb installment clamped to day 28, Apr to 30, Jun to 30, Sep to 30, Nov to 30, while Jan/Mar/May/Jul/Aug/Oct/Dec remain 31
- Test with `LEAP_YEAR_FEB` fixture: verify Feb 2024 clamped to day 29 (not 28)
- Test with `NON_LEAP_YEAR_FEB` fixture: verify Feb 2025 clamped to day 28

**FIN-02: year rollover (Dec to Jan next year)**
- Test with `YEAR_ROLLOVER` fixture (starts Oct 2025, 6 months): verify installments 1-3 are in 2025 (Oct, Nov, Dec), installments 4-6 are in 2026 (Jan, Feb, Mar)
- Verify monthLabel correctly shows the new year (e.g., "ENERO 2026" not "ENERO 2025")

**FIN-03: variable first installment amount**
- Test with `VARIABLE_FIRST_INSTALLMENT` fixture: verify installment[0].amount === 500, installments[1..11].amount === 250
- Test without firstInstallmentAmount: verify all installments have regularInstallmentAmount
- Use `expectMoney` for all amount assertions (FIN-09)

**FIN-04: zero installments (contado sale)**
- Test with `CONTADO_SALE` fixture (totalInstallments=0): verify result is an empty array
- Verify result.length === 0 explicitly

**Additional verifications across all tests:**
- Verify installmentNumber is sequential (1, 2, 3...)
- Verify monthLabel format is "MONTH_NAME YEAR" (e.g., "ENERO 2025")
- Verify currency passthrough with `ARS_CURRENCY` fixture (one test using `toGeneratorParams(ARS_CURRENCY, 'ars-sale', 'ARS')`)
- Use `expectMoney()` for EVERY amount assertion (FIN-09). Never use raw `toBe()` on amounts.
- Use `.getTime()` or `.getDate()` / `.getMonth()` / `.getFullYear()` for date comparisons, never reference equality on Date objects.
  </action>
  <verify>npx vitest run src/__tests__/unit/lib/installment-generator.test.ts --reporter=verbose 2>&1 (all tests pass)</verify>
  <done>Generator test suite passes with tests covering: day-31 clamping in all short months including Feb leap/non-leap (FIN-01), year rollover Dec-to-Jan (FIN-02), variable first installment amount (FIN-03), zero installments for contado (FIN-04), and all amount assertions use expectMoney (FIN-09)</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` -- all tests pass (original 9 + new generator tests)
2. Fixtures file exports all 8 named scenarios + toGeneratorParams
3. No raw `toBe()` on any amount value in the test file (grep for `toBe` should only appear on non-monetary values like dates, lengths, strings)
</verification>

<success_criteria>
- All FIN-01 date clamping scenarios pass (Feb 28, Feb 29, Apr 30, Jun 30, Sep 30, Nov 30)
- FIN-02 year rollover assertions pass (2025 to 2026 boundary)
- FIN-03 variable first installment verified
- FIN-04 contado returns empty array
- FIN-09 enforced: every monetary assertion uses expectMoney
- Shared fixtures file ready for consumption by Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/02-financial-logic-tests/02-01-SUMMARY.md`
</output>
