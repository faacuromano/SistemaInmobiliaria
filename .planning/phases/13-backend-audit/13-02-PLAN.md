---
phase: 13-backend-audit
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - docs/AUDIT_BACKEND.md
autonomous: true
requirements: [BACK-04, BACK-05]

must_haves:
  truths:
    - "A developer can find any signing action and read its complete step-by-step logic"
    - "A developer can find any person/user/development action and read its complete step-by-step logic"
    - "A developer can find any notification/message/audit-log/auth/import action and read its complete step-by-step logic"
    - "Every documented action includes: Archivo, Guard, Schema, Retorno+Errores, Modelos afectados metadata block"
    - "Service methods (import.service, development.service, signing.service) have their own detailed step-by-step sections"
    - "The alphabetical index table in section 2 is updated with correct section links (no more placeholders)"
    - "The document has a final statistics/summary section"
  artifacts:
    - path: "docs/AUDIT_BACKEND.md"
      provides: "Complete backend audit covering all 7 business domains"
      contains: "Firmas"
      min_lines: 1200
  key_links:
    - from: "docs/AUDIT_BACKEND.md section 7 (Firmas)"
      to: "docs/AUDIT_DATABASE.md section 3.17 (SigningSlot)"
      via: "Section cross-references"
      pattern: "AUDIT_DATABASE.md"
    - from: "docs/AUDIT_BACKEND.md section 10 (Sistema)"
      to: "docs/AUDIT_CONCEPT.md section 5.9 (Configuracion)"
      via: "Section cross-references"
      pattern: "AUDIT_CONCEPT.md"
---

<objective>
Complete docs/AUDIT_BACKEND.md by adding the remaining 4 business domains: Firmas (signings), Personas y Usuarios (persons, users), Desarrollos (developments, lots, tags), and Sistema (notifications, messages, receipts, audit log, auth, import, RBAC, business hours, system config). Update the alphabetical index table to replace placeholder section links with actual links. Add a final summary/statistics section.

Purpose: Complete the exhaustive backend audit document so every server action and service in the system is documented with full step-by-step logic, making the codebase fully understandable from documentation alone.

Output: docs/AUDIT_BACKEND.md grows from ~800-1200 lines to ~1800-2400+ lines with all 7 domains documented.
</objective>

<execution_context>
@D:/Proyectos/sistemaInmobiliaria/.claude/get-shit-done/workflows/execute-plan.md
@D:/Proyectos/sistemaInmobiliaria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-backend-audit/13-CONTEXT.md
@.planning/phases/13-backend-audit/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Firmas, Personas, Desarrollos, and Sistema modules</name>
  <files>docs/AUDIT_BACKEND.md</files>
  <read_first>
    - docs/AUDIT_BACKEND.md (current state from Plan 01 - understand existing structure, section numbering, format)
    - src/server/actions/signing.actions.ts (273 LOC - largest action file, signing slot management)
    - src/server/services/signing.service.ts (101 LOC - signing business logic)
    - src/schemas/signing.schema.ts (signing Zod schemas)
    - src/server/actions/person.actions.ts (220 LOC - person/client management)
    - src/schemas/person.schema.ts (person Zod schemas)
    - src/server/actions/user.actions.ts (211 LOC - user management)
    - src/schemas/user.schema.ts (user Zod schemas)
    - src/server/actions/development.actions.ts (121 LOC - development CRUD)
    - src/server/services/development.service.ts (141 LOC - development business logic)
    - src/schemas/development.schema.ts (development Zod schemas)
    - src/server/actions/tag.actions.ts (153 LOC - tag operations)
    - src/schemas/tag.schema.ts (tag Zod schemas)
    - src/server/actions/notification.actions.ts (130 LOC - notification management)
    - src/server/actions/message.actions.ts (97 LOC - messaging)
    - src/server/actions/payment-receipt.actions.ts (88 LOC - receipt actions)
    - src/server/actions/audit-log.actions.ts (84 LOC - audit logging)
    - src/server/actions/role-permission.actions.ts (83 LOC - RBAC definitions)
    - src/server/actions/business-hours.actions.ts (66 LOC - business hours)
    - src/schemas/business-hours.schema.ts (business hours Zod schemas)
    - src/server/actions/system-config.actions.ts (54 LOC - system config)
    - src/schemas/system-config.schema.ts (system config Zod schemas)
    - src/server/actions/auth.actions.ts (43 LOC - authentication)
    - src/schemas/auth.schema.ts (auth Zod schemas)
    - src/server/actions/import.actions.ts (29 LOC - import entry point)
    - src/server/services/import.service.ts (433 LOC - bulk import logic, largest service)
    - docs/AUDIT_DATABASE.md (for cross-reference section numbers)
    - docs/AUDIT_CONCEPT.md (for cross-reference section numbers)
  </read_first>
  <action>
Append the following sections to the existing docs/AUDIT_BACKEND.md, continuing the section numbering from Plan 01 (sections 1-6 already exist). The format MUST match exactly what was established in sections 4-6: same metadata block format (5 fields), same numbered step-by-step style, same cross-reference patterns. Written entirely in Spanish.

**Sections to add:**

```
## 7. Firmas (Signings)
### 7.1 signing.actions.ts
For each exported function: full metadata block + numbered step-by-step
- This is the largest action file (273 LOC) — expect 8-12 functions
- Document signing slot creation, availability checking, assignment to sales, completion flow
- Document the payment gate logic (signing blocked until payments are current)
- Document auto-commission generation on signing completion

### 7.2 signing.service.ts
For each exported method: full metadata block + numbered step-by-step
- Cross-reference: "esta funcion es llamada desde signing.actions.X (ver §7.1)"

## 8. Personas y Usuarios (Persons, Users)
### 8.1 person.actions.ts
For each exported function: full metadata block + numbered step-by-step
- Document CLIENTE/PROVEEDOR/AMBOS person type handling
- Document person-sale relationship queries

### 8.2 user.actions.ts
For each exported function: full metadata block + numbered step-by-step
- Document RBAC role assignment
- Document password hashing if present

## 9. Desarrollos (Developments, Lots, Tags)
### 9.1 development.actions.ts
For each exported function: full metadata block + numbered step-by-step

### 9.2 development.service.ts
For each exported method: full metadata block + numbered step-by-step
- Document lot bulk creation if present

### 9.3 tag.actions.ts
For each exported function: full metadata block + numbered step-by-step
- Document lot-tag relationship management

## 10. Comunicaciones (Notifications, Messages)
### 10.1 notification.actions.ts
For each exported function: full metadata block + numbered step-by-step
- Document NotificationType values and what triggers each

### 10.2 message.actions.ts
For each exported function: full metadata block + numbered step-by-step

## 11. Sistema (Auth, RBAC, Receipts, Audit, Import, Config)
### 11.1 auth.actions.ts
For each exported function: full metadata block + numbered step-by-step
- Document Auth.js v5 integration, session handling

### 11.2 role-permission.actions.ts
For each exported function: full metadata block + numbered step-by-step
- Document the 4 roles (SUPER_ADMIN, ADMINISTRACION, FINANZAS, COBRANZA) and permission definitions
- Document how guards use these definitions

### 11.3 payment-receipt.actions.ts
For each exported function: full metadata block + numbered step-by-step

### 11.4 audit-log.actions.ts
For each exported function: full metadata block + numbered step-by-step
- Document what data is captured per audit log entry
- Document query patterns for audit trail viewing

### 11.5 import.actions.ts + import.service.ts
For each exported function/method: full metadata block + numbered step-by-step
- import.service.ts is 433 LOC (largest service file) — give it thorough treatment
- Document CSV/Excel/JSON format handling
- Document validation, error collection, transaction behavior
- Document person import, lot import, sale import flows

### 11.6 business-hours.actions.ts
For each exported function: full metadata block + numbered step-by-step

### 11.7 system-config.actions.ts
For each exported function: full metadata block + numbered step-by-step

## 12. Resumen y Estadisticas
- Total server actions documented (count per module)
- Total service methods documented (count per module)
- Most complex actions (by step count)
- Guard/RBAC coverage summary (which roles guard which modules)
- Common patterns observed: prisma.$transaction usage, revalidatePath calls, audit logging patterns
- Cross-reference summary: links to AUDIT_CONCEPT.md, AUDIT_DATABASE.md
```

**Also update section 2 (Indice Alfabetico de Funciones):**
- Replace all placeholder section references for Plan 02 modules with actual section numbers
- Add any functions from Plan 02 modules that were missed in the initial index
- Verify alphabetical ordering is correct after additions

**Also update section 1 (Introduccion) and Tabla de Contenidos:**
- Update the TOC to include sections 7-12
- Update any statistics in the introduction (total actions, total services, etc.)
  </action>
  <verify>
    <automated>grep -c "^### \|^## " docs/AUDIT_BACKEND.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/AUDIT_BACKEND.md contains "## 7. Firmas" section with subsections 7.1 and 7.2
    - docs/AUDIT_BACKEND.md contains "## 8. Personas y Usuarios" section with subsections 8.1 and 8.2
    - docs/AUDIT_BACKEND.md contains "## 9. Desarrollos" section with subsections 9.1, 9.2, and 9.3
    - docs/AUDIT_BACKEND.md contains "## 10. Comunicaciones" section with subsections 10.1 and 10.2
    - docs/AUDIT_BACKEND.md contains "## 11. Sistema" section with subsections 11.1 through 11.7
    - docs/AUDIT_BACKEND.md contains "## 12. Resumen y Estadisticas" section
    - Every action function in signing.actions.ts (273 LOC, largest file) has a documented entry with 5 metadata fields
    - import.service.ts (433 LOC, largest service) has a detailed subsection with numbered steps
    - The index table in section 2 has NO placeholder section references remaining (all links are real)
    - Every documented function has all 5 metadata fields: Archivo, Guard, Schema, Retorno, Modelos afectados
    - Every documented function has numbered steps (1., 2., 3. minimum)
    - Document contains cross-references to "AUDIT_DATABASE.md" in sections 7-11
    - Document is at least 1200 lines total
    - Document is written entirely in Spanish
  </acceptance_criteria>
  <done>docs/AUDIT_BACKEND.md has all 12 sections covering all 7 business domains, every server action and service method is documented with metadata blocks and step-by-step flows, index table is complete with real section links, summary statistics section exists</done>
</task>

<task type="auto">
  <name>Task 2: Validate complete document coverage and accuracy</name>
  <files>docs/AUDIT_BACKEND.md</files>
  <read_first>
    - docs/AUDIT_BACKEND.md (the complete document)
    - src/server/actions/signing.actions.ts (verify all exported functions documented)
    - src/server/actions/person.actions.ts (verify all exported functions documented)
    - src/server/actions/user.actions.ts (verify all exported functions documented)
    - src/server/actions/development.actions.ts (verify all exported functions documented)
    - src/server/actions/tag.actions.ts (verify all exported functions documented)
    - src/server/actions/notification.actions.ts (verify all exported functions documented)
    - src/server/actions/message.actions.ts (verify all exported functions documented)
    - src/server/actions/payment-receipt.actions.ts (verify all exported functions documented)
    - src/server/actions/audit-log.actions.ts (verify all exported functions documented)
    - src/server/actions/role-permission.actions.ts (verify all exported functions documented)
    - src/server/actions/business-hours.actions.ts (verify all exported functions documented)
    - src/server/actions/system-config.actions.ts (verify all exported functions documented)
    - src/server/actions/auth.actions.ts (verify all exported functions documented)
    - src/server/actions/import.actions.ts (verify all exported functions documented)
    - src/server/services/import.service.ts (verify all exported methods documented)
    - src/server/services/development.service.ts (verify all exported methods documented)
    - src/server/services/signing.service.ts (verify all exported methods documented)
  </read_first>
  <action>
Run a systematic completeness check on the ENTIRE docs/AUDIT_BACKEND.md (not just Plan 02 sections — re-validate Plan 01 sections too):

**Check 1: Function coverage (ALL modules).** For each of the 21 action files and 7 service files:
- Extract every `export async function` or `export function` name from the source file
- Verify that function name appears in the document with its own entry
- If any function is missing, add it with full metadata block + step-by-step flow

**Check 2: Metadata completeness (ALL modules).** For every documented function across all sections 4-11:
- Verify presence of all 5 metadata fields: Archivo, Guard, Schema, Retorno + Errores, Modelos afectados
- If any field is missing, add it by reading the source file

**Check 3: Index table completeness.** The alphabetical index in section 2:
- Must include EVERY exported function from ALL 21 action files + ALL 7 service files
- Must have NO placeholder section references (all replaced with real section numbers)
- Must be sorted alphabetically
- Fix any missing entries, wrong section numbers, or sorting issues

**Check 4: Cross-reference accuracy.** Verify that:
- Every AUDIT_DATABASE.md section reference (e.g., "§3.7 Sale") points to the correct model section
- Every AUDIT_CONCEPT.md section reference points to the correct module section
- Every internal cross-reference (e.g., "Ver signing.service.X() en §7.2") points to a real subsection
- Fix any broken references

**Check 5: Section 12 statistics accuracy.** Verify that:
- Total action count matches actual number of documented actions
- Total service method count matches actual number of documented service methods
- Guard/RBAC coverage summary accurately lists which roles guard which modules

**Check 6: TOC completeness.** Verify Tabla de Contenidos lists all 12 sections with correct sub-section entries.

Fix any gaps found. If no gaps are found, document that validation passed.
  </action>
  <verify>
    <automated>wc -l docs/AUDIT_BACKEND.md</automated>
  </verify>
  <acceptance_criteria>
    - Every exported function from ALL 21 action files appears in docs/AUDIT_BACKEND.md
    - Every exported function from ALL 7 service files appears in docs/AUDIT_BACKEND.md
    - Every documented function has all 5 metadata fields (Archivo, Guard, Schema, Retorno, Modelos afectados)
    - Every documented function has numbered steps (1., 2., 3. minimum)
    - Index table in section 2 has zero placeholder references
    - Index table is sorted alphabetically
    - All cross-references to AUDIT_DATABASE.md and AUDIT_CONCEPT.md point to valid sections
    - All internal cross-references (action -> service, service -> action) point to valid sections
    - Section 12 statistics match actual counts
    - Document is at least 1200 lines total
    - Tabla de Contenidos covers all 12 sections
  </acceptance_criteria>
  <done>Complete docs/AUDIT_BACKEND.md validated — all 21 action files and 7 service files fully documented, zero missing functions, zero missing metadata fields, zero broken cross-references, accurate statistics, complete index table</done>
</task>

</tasks>

<verification>
- docs/AUDIT_BACKEND.md exists with sections 1-12 fully written
- All 21 server action files documented exhaustively
- All 7 service files documented exhaustively
- Alphabetical index table complete with real section links (no placeholders)
- Every function has 5-field metadata block + numbered step-by-step flow
- Cross-references to AUDIT_DATABASE.md and AUDIT_CONCEPT.md are accurate
- Summary statistics in section 12 are accurate
- Document is at least 1200 lines
</verification>

<success_criteria>
- A developer can find any server action or service method in the system using the alphabetical index and immediately navigate to its full documentation
- The document covers all 7 business domains: Ventas, Pagos, Caja, Firmas, Personas/Usuarios, Desarrollos, Sistema
- Every documented function includes enough detail that a developer who has never seen the source code can understand what it does, what it validates, what DB operations it performs, and what it returns
- The document is self-contained enough to read independently, with inline model descriptions and cross-references to AUDIT_DATABASE.md and AUDIT_CONCEPT.md for deeper detail
</success_criteria>

<output>
After completion, create `.planning/phases/13-backend-audit/13-02-SUMMARY.md`
</output>
