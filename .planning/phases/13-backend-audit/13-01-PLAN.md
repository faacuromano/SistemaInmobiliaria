---
phase: 13-backend-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/AUDIT_BACKEND.md
autonomous: true
requirements: [BACK-01, BACK-02, BACK-03, BACK-05]

must_haves:
  truths:
    - "A developer can find any sale action (createSale, updateSale, cancelSale, etc.) and read its complete step-by-step logic"
    - "A developer can find any payment/installment/extra-charge action and read its complete step-by-step logic"
    - "A developer can find any cash-movement/cash-balance/exchange-rate action and read its complete step-by-step logic"
    - "Every documented action includes: Archivo, Guard, Schema, Retorno+Errores, Modelos afectados metadata block"
    - "Service methods (sale.service, payment.service, extra-charge.service, receipt.service) have their own detailed step-by-step sections"
    - "An alphabetical quick-reference index table exists at the top mapping every function to its file, module, and section link"
  artifacts:
    - path: "docs/AUDIT_BACKEND.md"
      provides: "Backend audit document covering Ventas, Pagos y Caja domains"
      contains: "AUDIT_BACKEND"
  key_links:
    - from: "docs/AUDIT_BACKEND.md"
      to: "docs/AUDIT_DATABASE.md"
      via: "Section cross-references"
      pattern: "AUDIT_DATABASE.md"
    - from: "docs/AUDIT_BACKEND.md"
      to: "docs/AUDIT_CONCEPT.md"
      via: "Section cross-references"
      pattern: "AUDIT_CONCEPT.md"
---

<objective>
Create the first half of docs/AUDIT_BACKEND.md covering the three heaviest business domains: Ventas (sales), Pagos y Cuotas (payments, installments, extra charges), and Caja (cash movements, balances, exchange rates). This includes the document skeleton (table of contents, intro, alphabetical index table) plus exhaustive documentation of all server actions and services in these domains.

Purpose: These three domains contain the most complex business logic in the system (sale creation with installment generation, payment processing with currency conversion, cash movement tracking). Documenting them first creates the foundation for the complete backend audit.

Output: docs/AUDIT_BACKEND.md with ~800-1200 lines covering document structure + 3 domain modules + their services.
</objective>

<execution_context>
@D:/Proyectos/sistemaInmobiliaria/.claude/get-shit-done/workflows/execute-plan.md
@D:/Proyectos/sistemaInmobiliaria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-backend-audit/13-CONTEXT.md
@.planning/phases/12-database-architecture/12-01-SUMMARY.md
@.planning/phases/11-conceptual-overview/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Ventas module (sale actions + sale.service)</name>
  <files>docs/AUDIT_BACKEND.md</files>
  <read_first>
    - src/server/actions/sale.actions.ts (98 LOC - all sale server actions)
    - src/server/services/sale.service.ts (333 LOC - heavy sale business logic)
    - src/schemas/sale.schema.ts (Zod validation schemas used by sale actions)
    - src/server/actions/lot.actions.ts (156 LOC - lot operations called during sales)
    - src/schemas/lot.schema.ts (Zod schemas for lot operations)
    - src/lib/installment-recalculator.ts (82 LOC - installment recalculation logic)
    - src/lib/audit.ts (31 LOC - audit utility)
    - src/lib/service-error.ts (10 LOC - error handling)
    - src/server/actions/payment.actions.ts (172 LOC - needed to understand payment flow)
    - src/server/services/payment.service.ts (392 LOC - payment business logic)
    - src/server/services/receipt.service.ts (176 LOC - receipt generation)
    - src/server/actions/extra-charge.actions.ts (127 LOC - extra charge actions)
    - src/server/services/extra-charge.service.ts (140 LOC - extra charge logic)
    - src/server/actions/cash-movement.actions.ts (181 LOC - cash movement ops)
    - src/server/actions/cash-balance.actions.ts (133 LOC - cash balance)
    - src/server/actions/exchange-rate.actions.ts (136 LOC - exchange rate)
    - src/schemas/cash-movement.schema.ts (cash movement Zod schemas)
    - src/schemas/exchange-rate.schema.ts (exchange rate Zod schemas)
    - src/schemas/extra-charge.schema.ts (extra charge Zod schemas)
    - docs/AUDIT_CONCEPT.md (Phase 11 output - for cross-reference section numbers)
    - docs/AUDIT_DATABASE.md (Phase 12 output - for cross-reference section numbers)
    - src/server/actions/role-permission.actions.ts (83 LOC - to understand guard definitions)
    - src/server/actions/auth.actions.ts (43 LOC - to understand auth flow)
  </read_first>
  <action>
Create the file `docs/AUDIT_BACKEND.md` with the following structure and content. The entire document MUST be written in Spanish (consistent with Phase 11 and Phase 12 audit documents).

**Document skeleton (write first, then fill):**

```
# Auditoria de Backend: Server Actions y Services

## Tabla de Contenidos
[numbered TOC linking to all sections]

## 1. Introduccion
- Purpose: explain what this document covers (every server action and service, step-by-step)
- Architecture recap: Pages -> Server Actions -> Services -> Models -> Prisma
- How to read this document: explain the metadata block format and numbered step format
- Cross-references: mention AUDIT_CONCEPT.md and AUDIT_DATABASE.md with section links

## 2. Indice Alfabetico de Funciones
[Table with columns: Funcion | Archivo | Modulo | Seccion]
- List EVERY exported function from ALL 21 action files and ALL 7 service files
- This is a complete index for the entire document (including Plan 02 modules)
- For Plan 02 modules not yet documented, include them in the index with section placeholders
- Sort alphabetically by function name

## 3. Utilidades Compartidas
- Document src/lib/service-error.ts: ServiceError class, constructor signature, usage pattern
- Document src/lib/audit.ts: logAction/logActionFromSession helpers, parameters, when they're called
- Document src/lib/exchange-rate.ts: fetchExchangeRate function, dolarapi.com integration, caching
- Document src/lib/installment-recalculator.ts: recalculateInstallments function, algorithm description

## 4. Ventas (Sales)
### 4.1 sale.actions.ts
For each exported function (createSale, updateSale, cancelSale, getSales, getSaleById, etc.):
- Metadata block:
  **Archivo:** src/server/actions/sale.actions.ts L{start}-L{end}
  **Guard:** requireRole() args (exact roles)
  **Schema:** exact Zod schema name from sale.schema.ts
  **Retorno:** return type shape + error conditions
  **Modelos afectados:** which Prisma models are read/written (with links to AUDIT_DATABASE.md section numbers)
- Numbered step-by-step flow: 1. Parse/validate input with Zod -> 2. Auth guard -> 3. Business logic -> 4. DB operations -> 5. Audit log -> 6. revalidatePath -> 7. Return

### 4.2 sale.service.ts
For each exported method (createSale, updateSale, cancelSale, etc.):
- Same metadata block format
- Detailed step-by-step including: prisma.$transaction usage, installment generation algorithm (how many, amounts, due dates), lot status transitions, audit logging
- Cross-reference: "esta funcion es llamada desde sale.actions.createSale (ver §4.1)"

### 4.3 lot.actions.ts
For each exported function (getLots, updateLot, deleteLot, etc.):
- Same metadata + step-by-step format
- Note which lot status transitions are business-critical

## 5. Pagos y Cuotas (Payments, Installments, Extra Charges)
### 5.1 payment.actions.ts
- Same format for each function
- Emphasize currency conversion logic

### 5.2 payment.service.ts
- Detailed step-by-step for processPayment, including:
  - Exchange rate lookup
  - Partial payment handling
  - Installment status transition (PENDIENTE -> PAGADA)
  - CashMovement creation
  - Receipt generation trigger
  - Audit logging

### 5.3 extra-charge.actions.ts
- Same format for each function

### 5.4 extra-charge.service.ts
- Detailed step-by-step including installment recalculation trigger

### 5.5 receipt.service.ts
- Receipt generation logic, template rendering, auto-generation triggers

## 6. Caja y Cotizacion (Cash Movements, Balances, Exchange Rates)
### 6.1 cash-movement.actions.ts
- Same format for each function
- Document all 14+ MovementType values and what triggers each

### 6.2 cash-balance.actions.ts
- Same format for each function

### 6.3 exchange-rate.actions.ts
- Same format for each function
- Document dolarapi.com integration, caching strategy, fallback behavior
```

**Critical formatting rules (from CONTEXT.md decisions):**
1. Each action's metadata block has exactly 5 fields: Archivo, Guard, Schema, Retorno + Errores, Modelos afectados
2. Step-by-step flow as numbered list (not table format)
3. Full file path on first mention, short name after
4. Same depth for ALL actions including simple CRUD
5. When an action delegates to a service: "-> Ver {service}.{method}() en §X.Y"
6. On first mention of a model: brief inline note with AUDIT_DATABASE.md section reference
7. For the index table: include ALL functions from ALL modules (even those documented in Plan 02) with section placeholders for not-yet-written sections

**For the alphabetical index table:** Read ALL 21 action files and ALL 7 service files to extract every exported function name. Include functions from modules that will be documented in Plan 02 (signing, person, user, development, tag, notification, message, payment-receipt, audit-log, role-permission, business-hours, system-config, auth, import) with section placeholder references like "§7.1", "§8.1", etc.
  </action>
  <verify>
    <automated>grep -c "^### \|^## " docs/AUDIT_BACKEND.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/AUDIT_BACKEND.md exists and is non-empty
    - File contains "# Auditoria de Backend" as the title
    - File contains "## 2. Indice Alfabetico de Funciones" section with a markdown table
    - File contains "## 3. Utilidades Compartidas" section
    - File contains "## 4. Ventas" section with subsections 4.1 (sale.actions), 4.2 (sale.service), 4.3 (lot.actions)
    - File contains "## 5. Pagos y Cuotas" section with subsections 5.1 through 5.5
    - File contains "## 6. Caja y Cotizacion" section with subsections 6.1 through 6.3
    - Every action function has a metadata block containing "**Archivo:**", "**Guard:**", "**Schema:**", "**Retorno**", "**Modelos afectados:**"
    - Every action function has numbered steps (at least "1." and "2." present)
    - File contains cross-references to "AUDIT_DATABASE.md" (at least 5 occurrences)
    - File contains cross-references to "AUDIT_CONCEPT.md" (at least 2 occurrences)
    - Index table contains at least 40 rows (covering functions from all modules)
    - sale.service.ts has its own subsection with detailed step-by-step
    - payment.service.ts has its own subsection with detailed step-by-step
    - Document is written in Spanish (contains "Validar", "Guardar", "Retorno" etc.)
    - File is at least 600 lines long
  </acceptance_criteria>
  <done>docs/AUDIT_BACKEND.md exists with complete documentation of Ventas (sale.actions + sale.service + lot.actions), Pagos y Cuotas (payment.actions + payment.service + extra-charge.actions + extra-charge.service + receipt.service), and Caja y Cotizacion (cash-movement.actions + cash-balance.actions + exchange-rate.actions) modules, plus alphabetical index table covering ALL modules, plus shared utilities section</done>
</task>

<task type="auto">
  <name>Task 2: Validate Plan 01 document completeness</name>
  <files>docs/AUDIT_BACKEND.md</files>
  <read_first>
    - docs/AUDIT_BACKEND.md (the document just created in Task 1)
    - src/server/actions/sale.actions.ts (verify all exported functions are documented)
    - src/server/actions/payment.actions.ts (verify all exported functions are documented)
    - src/server/actions/extra-charge.actions.ts (verify all exported functions are documented)
    - src/server/actions/cash-movement.actions.ts (verify all exported functions are documented)
    - src/server/actions/cash-balance.actions.ts (verify all exported functions are documented)
    - src/server/actions/exchange-rate.actions.ts (verify all exported functions are documented)
    - src/server/actions/lot.actions.ts (verify all exported functions are documented)
    - src/server/services/sale.service.ts (verify all exported methods are documented)
    - src/server/services/payment.service.ts (verify all exported methods are documented)
    - src/server/services/extra-charge.service.ts (verify all exported methods are documented)
    - src/server/services/receipt.service.ts (verify all exported methods are documented)
  </read_first>
  <action>
Run a systematic completeness check on docs/AUDIT_BACKEND.md:

**Check 1: Function coverage.** For each of the 7 action files (sale, payment, extra-charge, cash-movement, cash-balance, exchange-rate, lot) and 4 service files (sale.service, payment.service, extra-charge.service, receipt.service):
- Extract every `export async function` or `export function` name from the source file
- Verify that function name appears in the document with its own subsection or detailed entry
- If any function is missing, add it with full metadata block + step-by-step

**Check 2: Metadata completeness.** For every documented function, verify presence of all 5 metadata fields:
- **Archivo:** (must include file path and line range)
- **Guard:** (must say specific role or "Ninguno" or "Session requerida")
- **Schema:** (must name specific Zod schema or "Ninguno")
- **Retorno + Errores:** (must describe return shape and error conditions)
- **Modelos afectados:** (must list Prisma model names)
If any metadata field is missing, add it by reading the source file.

**Check 3: Step-by-step completeness.** Every documented function must have numbered steps. Verify that:
- Simple CRUD functions have at least 3 steps (validate, query, return)
- Complex functions (createSale, processPayment, cancelSale) have at least 5 steps
- Steps accurately reflect the source code logic (compare numbered steps against actual code flow)

**Check 4: Cross-reference accuracy.** Verify that:
- Every AUDIT_DATABASE.md section reference (e.g., "§3.7") actually matches the correct model in that document
- Every service cross-reference (e.g., "Ver sale.service.createSale() en §4.2") points to a real section

**Check 5: Index table completeness.** The alphabetical index in section 2 must include:
- Every function from the 7 action files documented in this plan
- Placeholder entries for functions from the 14 action files + 3 service files that will be documented in Plan 02
- Correct section links for documented functions, placeholder section links for Plan 02 functions

Fix any gaps found. If no gaps are found, document that the validation passed with no changes needed.
  </action>
  <verify>
    <automated>grep -c "export.*function\|export.*async function" src/server/actions/sale.actions.ts src/server/actions/payment.actions.ts src/server/actions/extra-charge.actions.ts src/server/actions/cash-movement.actions.ts src/server/actions/cash-balance.actions.ts src/server/actions/exchange-rate.actions.ts src/server/actions/lot.actions.ts src/server/services/sale.service.ts src/server/services/payment.service.ts src/server/services/extra-charge.service.ts src/server/services/receipt.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every exported function from the 7 action files (sale, payment, extra-charge, cash-movement, cash-balance, exchange-rate, lot) appears in docs/AUDIT_BACKEND.md
    - Every exported function from the 4 service files (sale.service, payment.service, extra-charge.service, receipt.service) appears in docs/AUDIT_BACKEND.md
    - Every documented function has all 5 metadata fields present (Archivo, Guard, Schema, Retorno, Modelos afectados)
    - Every documented function has numbered steps (1., 2., 3. minimum)
    - Index table in section 2 has entries for ALL modules (including Plan 02 placeholder entries)
    - Cross-references to AUDIT_DATABASE.md sections are accurate
  </acceptance_criteria>
  <done>All exported functions from Plan 01 scope are documented with complete metadata blocks and step-by-step flows; index table covers all modules; cross-references verified accurate</done>
</task>

</tasks>

<verification>
- docs/AUDIT_BACKEND.md exists with sections 1-6 fully written
- Alphabetical index table covers all 21 action files + 7 service files (with placeholders for Plan 02 content)
- Every action in sale.actions.ts, payment.actions.ts, extra-charge.actions.ts, cash-movement.actions.ts, cash-balance.actions.ts, exchange-rate.actions.ts, lot.actions.ts is documented
- Every method in sale.service.ts, payment.service.ts, extra-charge.service.ts, receipt.service.ts is documented
- All documented functions have 5-field metadata blocks and numbered step-by-step flows
- Cross-references to AUDIT_DATABASE.md and AUDIT_CONCEPT.md are present and accurate
</verification>

<success_criteria>
- docs/AUDIT_BACKEND.md is at least 600 lines and covers 3 complete business domains
- A developer reading the Ventas section can trace any sale operation from action entry point through service logic to DB operations
- A developer reading the Pagos section can trace any payment operation including currency conversion, partial payments, receipt generation
- A developer reading the Caja section can understand all cash movement types and how balances are computed
- The alphabetical index table allows finding any function in the system in under 10 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/13-backend-audit/13-01-SUMMARY.md`
</output>
