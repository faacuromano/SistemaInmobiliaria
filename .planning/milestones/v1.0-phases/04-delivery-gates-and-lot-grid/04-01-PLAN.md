---
phase: 04-delivery-gates-and-lot-grid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/unit/lib/installment-recalculator.test.ts
  - src/__tests__/unit/lib/installment-preview-parity.test.ts
  - src/__tests__/integration/payment-actions.test.ts
  - src/__tests__/integration/sale-actions.test.ts
  - src/app/(dashboard)/caja/page.tsx
  - src/app/(dashboard)/mensajes/_components/message-list.tsx
  - src/app/(dashboard)/ventas/[id]/_components/pay-installment-dialog.tsx
  - src/app/(dashboard)/ventas/[id]/_components/receipts-section.tsx
  - src/components/shared/notification-bell.tsx
  - src/lib/email.ts
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03

must_haves:
  truths:
    - "`tsc --noEmit` exits with code 0 and zero type errors"
    - "`npm run lint` exits with code 0, zero errors, zero warnings"
    - "`npm run build` completes successfully with exit code 0"
  artifacts:
    - path: "src/__tests__/unit/lib/installment-recalculator.test.ts"
      provides: "Type-safe expectMoney calls with Number() casts"
    - path: "src/__tests__/integration/payment-actions.test.ts"
      provides: "File-level eslint-disable for no-explicit-any in test mocks"
    - path: "src/__tests__/integration/sale-actions.test.ts"
      provides: "File-level eslint-disable for no-explicit-any in test mocks"
  key_links:
    - from: "GATE-02 (lint fixes)"
      to: "GATE-03 (build)"
      via: "Next.js runs ESLint during build — lint fixes unblock build"
      pattern: "npm run build.*eslint"
---

<objective>
Fix all TypeScript type errors, ESLint errors, and ESLint warnings so the three delivery gates pass clean: `tsc --noEmit`, `npm run lint`, and `npm run build`.

Purpose: The build currently fails because Next.js runs ESLint during build. Fixing the 6 type errors, 41 lint errors, and 6 lint warnings unblocks a clean build artifact for client delivery.
Output: All three gate commands pass with zero errors/warnings.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-delivery-gates-and-lot-grid/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TypeScript type errors in test files</name>
  <files>
    src/__tests__/unit/lib/installment-recalculator.test.ts
  </files>
  <action>
Fix the 6 `tsc --noEmit` errors in `installment-recalculator.test.ts`. All 6 are the same pattern: `expectMoney` receives a Prisma `Decimal | string | number | ... | undefined` union instead of `number`.

For each `expectMoney` call that passes a Prisma update argument (e.g., `callArgs.data.amount`, `callArgs.data.originalAmount`), wrap the value with `Number()`:

```typescript
// Before (fails tsc):
expectMoney(callArgs.data.amount, 150)

// After (passes tsc):
expectMoney(Number(callArgs.data.amount), 150)
```

Find all `expectMoney` calls in the file that receive mock Prisma update args and apply the `Number()` wrapper. Do NOT change calls that already pass plain numbers.

Run `npx tsc --noEmit` after changes to verify zero type errors remain.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    `tsc --noEmit` exits with code 0 and reports zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix all ESLint errors and warnings across codebase</name>
  <files>
    src/__tests__/unit/lib/installment-recalculator.test.ts
    src/__tests__/unit/lib/installment-preview-parity.test.ts
    src/__tests__/integration/payment-actions.test.ts
    src/__tests__/integration/sale-actions.test.ts
    src/app/(dashboard)/caja/page.tsx
    src/app/(dashboard)/mensajes/_components/message-list.tsx
    src/app/(dashboard)/ventas/[id]/_components/pay-installment-dialog.tsx
    src/app/(dashboard)/ventas/[id]/_components/receipts-section.tsx
    src/components/shared/notification-bell.tsx
    src/lib/email.ts
  </files>
  <action>
Fix all 41 ESLint errors and 6 ESLint warnings using these strategies:

**Test file `as any` errors (41 errors total):**
Add a file-level ESLint disable comment at the TOP of each test file (after any existing imports):

```typescript
/* eslint-disable @typescript-eslint/no-explicit-any */
```

Apply to these 4 test files:
- `src/__tests__/integration/payment-actions.test.ts` (21 instances)
- `src/__tests__/integration/sale-actions.test.ts` (9 instances)
- `src/__tests__/unit/lib/installment-recalculator.test.ts` (10 instances)

This is standard practice for test files with extensive Prisma mocking where full typing would be excessive.

**Production `as any` (1 error in `caja/page.tsx`):**
Find the `initialBalances as any` cast and replace with a proper type. Look at what the variable is being passed to — likely a component prop. Use the actual expected type or add `// eslint-disable-next-line @typescript-eslint/no-explicit-any` with a comment explaining why (if the type is genuinely complex).

**Unused variables (5 warnings):**
- `src/app/(dashboard)/mensajes/_components/message-list.tsx`: Find `isPending` — if it's from a `useTransition` call and not used, prefix with `_isPending` or remove the destructured variable
- `src/app/(dashboard)/ventas/[id]/_components/pay-installment-dialog.tsx`: Find `isPartial` — prefix with `_` or remove if unused
- `src/app/(dashboard)/ventas/[id]/_components/receipts-section.tsx`: Find `canManage` — prefix with `_` or remove if unused
- `src/components/shared/notification-bell.tsx`: Find unused `Check` import from lucide-react — remove it from the import statement

**Stale ESLint directive (1 warning):**
- `src/lib/email.ts`: Find a `// eslint-disable-next-line` comment that no longer applies (the code below it was changed to use dynamic `import()` instead of `require()`). Remove the stale directive line.

**Also from Task 1 file:** The `installment-recalculator.test.ts` file already gets the file-level eslint-disable from this task, which also covers the `no-explicit-any` errors there.

**`installment-preview-parity.test.ts`:** Find `label` defined but never used — prefix with `_label` or remove it.

After all changes, run `npm run lint` and verify zero errors AND zero warnings.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    `npm run lint` exits with code 0, reporting zero errors and zero warnings. `npm run build` completes successfully (since lint was the only build blocker).
  </done>
</task>

</tasks>

<verification>
Run all three delivery gate commands in sequence:

```bash
npx tsc --noEmit && npm run lint && npm run build
```

All three must exit with code 0. The build must produce a `.next` output directory.

Also verify existing tests still pass:
```bash
npm run test:run
```
</verification>

<success_criteria>
1. `tsc --noEmit` completes with zero type errors
2. `npm run lint` completes with zero warnings and zero errors
3. `npm run build` completes successfully and produces a deployable artifact
4. All existing tests continue to pass (`npm run test:run`)
</success_criteria>

<output>
After completion, create `.planning/phases/04-delivery-gates-and-lot-grid/04-01-SUMMARY.md`
</output>
